export const storePickupLocation = (location) => {
    return {
        type: 'STORE_PICKUP_LOCATION',
        payload: location
    }
}

export const storeDropoffLocation = (location) => {
    return {
        type: 'STORE_PICKUP_LOCATION',
        payload: location
    }
}